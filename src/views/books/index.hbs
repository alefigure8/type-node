{{#if books}}
{{#each books}}
<div class="col-md-4 mb-2">
    <div class="card card-body">
    <h3>{{title}}</h3>
    <p>Author: {{title}}</p>
    <p>ISBN: {{isbn}}</p>
</div>
</div>
{{/each}}
{{else}}
<p>No hay libros</p>
{{/ if}}